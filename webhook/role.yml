---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: prometheus-webhook

rules:
  - apiGroups:
      - ""
    resources:
      - "secrets"
    verbs:
      - "get"
      - "create"

...
