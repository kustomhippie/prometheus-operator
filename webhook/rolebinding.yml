---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: prometheus-webhook

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: prometheus-webhook

subjects:
  - kind: ServiceAccount
    name: prometheus-webhook

...
